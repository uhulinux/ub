diff -Naur orig/configure patched/configure
--- orig/configure	2015-01-20 18:47:20.000000000 +0100
+++ patched/configure	2015-01-20 20:13:20.124328772 +0100
@@ -4424,7 +4424,7 @@
 
 GETTEXT_PACKAGE="${PACKAGE}"
 
-localedir='$(prefix)/$(DATADIRNAME)/locale'
+localedir='$(datadir)/locale'
 
 
 # Workaround to make aclocal get the right flags
diff -Naur orig/configure.ac patched/configure.ac
--- orig/configure.ac	2015-01-20 18:46:56.000000000 +0100
+++ patched/configure.ac	2015-01-20 20:13:20.124328772 +0100
@@ -11,7 +11,7 @@
 
 GETTEXT_PACKAGE="${PACKAGE}"
 AC_SUBST(GETTEXT_PACKAGE)
-localedir='$(prefix)/$(DATADIRNAME)/locale'
+localedir='$(datadir)/locale'
 AC_SUBST(localedir)
 
 # Workaround to make aclocal get the right flags
diff -Naur orig/mate/Makefile patched/mate/Makefile
--- orig/mate/Makefile	2015-01-20 18:47:22.000000000 +0100
+++ patched/mate/Makefile	2015-01-20 20:13:20.124328772 +0100
@@ -256,7 +256,7 @@
 intltool__v_merge_options_0 = -q
 libdir = ${exec_prefix}/lib
 libexecdir = ${exec_prefix}/libexec
-localedir = $(prefix)/$(DATADIRNAME)/locale
+localedir = $(datadir)/locale
 localstatedir = ${prefix}/var
 mandir = ${datarootdir}/man
 mkdir_p = $(MKDIR_P)
diff -Naur orig/mate/scalable/Makefile patched/mate/scalable/Makefile
--- orig/mate/scalable/Makefile	2015-01-20 18:47:22.000000000 +0100
+++ patched/mate/scalable/Makefile	2015-01-20 20:13:20.124328772 +0100
@@ -197,7 +197,7 @@
 intltool__v_merge_options_0 = -q
 libdir = ${exec_prefix}/lib
 libexecdir = ${exec_prefix}/libexec
-localedir = $(prefix)/$(DATADIRNAME)/locale
+localedir = $(datadir)/locale
 localstatedir = ${prefix}/var
 mandir = ${datarootdir}/man
 mkdir_p = $(MKDIR_P)
diff -Naur orig/menta/Makefile patched/menta/Makefile
--- orig/menta/Makefile	2015-01-20 18:47:22.000000000 +0100
+++ patched/menta/Makefile	2015-01-20 20:13:20.124328772 +0100
@@ -197,7 +197,7 @@
 intltool__v_merge_options_0 = -q
 libdir = ${exec_prefix}/lib
 libexecdir = ${exec_prefix}/libexec
-localedir = $(prefix)/$(DATADIRNAME)/locale
+localedir = $(datadir)/locale
 localstatedir = ${prefix}/var
 mandir = ${datarootdir}/man
 mkdir_p = $(MKDIR_P)
